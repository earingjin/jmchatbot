# LLM provider: "mock" (no API key needed, runs a scripted demo flow) or "anthropic"
LLM_PROVIDER=mock

# Required only when LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

# Password gate for /admin/dashboard (MVP-level auth — see README)
ADMIN_PASSWORD=change-me
